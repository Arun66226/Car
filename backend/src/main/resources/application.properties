# Spring Boot Server Port
server.port=8080

# Database Configuration (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/mishra_travels?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=7423
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configuration
# CHANGE: Set to 'update' so it uses the Workbench tables without dropping them
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Enable detailed error messages in JSON responses
server.error.include-message=always
server.error.include-binding-errors=always

# JWT Configuration (Secret key must be at least 256 bits / 32 bytes)
app.jwt.secret=9a67a86c75f3a09e0231db62184d0b135b3401ef942289cf29188a1b52a1d0f5
app.jwt.expiration-ms=86400000